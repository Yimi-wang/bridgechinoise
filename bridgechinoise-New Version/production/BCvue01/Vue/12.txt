       if (e.getSource() == AIModeRightButton) {
            try {
                AISet();
            } catch (IOException ex) {
                throw new RuntimeException(ex);
            }
            exit(0);
        }

                if (e.getSource() == GameModeRightButton) {
                    try {
                        GamemodeSet();
                    } catch (IOException ex) {
                        throw new RuntimeException(ex);
                    }

                    AIModeAppear();
                    gameModeDisappear();
                }

           public void GamemodeSet() throws IOException {
               FileWriter fw;
               System.out.println(GameModeComboBox.getSelectedIndex());
               fw = new FileWriter("./bridgechinoise-New Version/bridgechinoise/res/default.cfg", false);
               fw.write("GameMode=" + GameModeComboBox.getSelectedIndex() + "\n");
               if (GameModeComboBox.getSelectedIndex() > 1) {
                  //
               }
               fw.close();
           }

           public void AISet() throws IOException {FileWriter fw;
               System.out.println(GameModeComboBox.getSelectedIndex());
               fw = new FileWriter("./bridgechinoise-New Version/bridgechinoise/res/default.cfg", true);
               fw.write("AI=" + AIModeComboBox.getSelectedIndex() + "\n");
               fw.close();}
